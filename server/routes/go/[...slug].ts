import { SeoComposition } from "#sg/seo";

// Аффилиатный редирект отдаётся сервером, без Vue-страницы:
// без JS, без гидрации, ~50 мс до перехода.
// 302 (а не 301) — партнёрская ссылка может смениться в любой момент,
// а 301 браузер закэшировал бы бессрочно.
// Catch-all, потому что ссылки идут со слешем на конце (/go/<slug>/),
// а url_normalize этот префикс не трогает.
export default SeoComposition.GetRefLinkRedirect;
